# predict_tails.py config — KG expansion using BERT-init stage-2 GraphMERT checkpoint
# Eval split (ch8): covers 39 heads not in the ch1-7 training split.

model_path: DELLA_ROOT/outputs/networking_graphmert_bert_init_stage2/span/rel22/bs32_lr_2e-05
tokenizer_path: BERT_TOKENIZER_PATH

preprocessed_dataset_path: DELLA_ROOT/gen4_triplets/predict_tails_input/eval
relation_map_path: DELLA_ROOT/gen4_triplets/relation_map.json

output_dir: DELLA_ROOT/outputs/kg_expansion_bert_init_stage2/eval

top_k: 15
batch_size: 8

num_leaves: 3
root_nodes: 128

num_parallel_jobs: 1
take_subset: false
